<html>
  <head>
    <title>The Gossip Project</title>
  </head>
  <body>
    <h1>Page potin</h1>
    <p>Voici la page du potin numéro <%= params["id"].to_i + 1 %></p>
    <p>Son auteur est <%= gossip_n.author %></p>
    <p>Contenu du potin : <%= gossip_n.content %></p>
    <p><a href="/gossips/<%= id %>/edit/">Editer ce potin</a></p>
    <p>Entrer un nouveau commentaire : </p>
    <form action='/gossips/<%= id %>/' method='POST'>
      Ton nom :  <input type='text' name='comment_author'/><br/>
      Ton commentaire : <input type='text' name='comment_content'/><br/>
      <input type='submit'/>
    </form>
    <p>Commentaires</p>
    <% comments.each do |comment| %>
      <% if comment.id == params["id"] %>
        <p>
          <%= comment.author %><br/>
          <%= comment.content %>
        </p>
        <% end %>
      <% end %>
    <p><a href="/">Revenir à l'accueil</a></p>
  </body>
</html>